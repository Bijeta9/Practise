//arrays are data structures that are used to..
// arrays are used to store ordered collection or list of data
//array is an object so it has key value pairs so the index is its key with the values given

let productColors = ['blue', 'white'];
console.log(productColors);

productColors.push("hello");
productColors[0] = 42;
console.log(productColors[0]);
console.log(typeof(productColors)); //gives object as the datatype


//if we just want to access any one from the array
console.log(productColors[0]);

//properties that are used with arrays
console.log(productColors.length);

console.log(productColors);

